---
volumes:
  nordlynx_config:

# networks:
#   networks_vpn_net:
#     external: true
#   networks_infra_net:
#     external: true

services:
  # 🔒 NordLynx (WireGuard de NordVPN)
  nordlynx:
    image: ghcr.io/bubuntux/nordlynx:latest
    container_name: nordlynx
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
    environment:
      - PRIVATE_KEY=${NORDLYNX_PRIVATE_KEY}
    volumes:
      - nordlynx_config:/config
    # networks:
    #   - networks_vpn_net
    #   - networks_infra_net

  # # 💾 qBittorrent — ruteado por NordLynx
  # qbittorrent:
  #   image: lscr.io/linuxserver/qbittorrent:latest
  #   container_name: qbittorrent
  #   network_mode: service:nordlynx
  #   restart: unless-stopped
  #   environment:
  #     - PUID=1000
  #     - PGID=1000
  #     - TZ=America/Mexico_City
  #     - WEBUI_PORT=8080
  #   volumes:
  #     - qbittorrent_config:/config
  #     - /mnt/media/downloads:/downloads
  #   depends_on:
  #     - nordlynx
